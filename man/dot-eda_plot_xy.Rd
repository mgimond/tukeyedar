% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_plot_xy.R
\name{.eda_plot_xy}
\alias{.eda_plot_xy}
\title{x-y Scatterplot with EDA Enhancements}
\usage{
.eda_plot_xy(
  dat,
  x,
  y,
  xlab = NULL,
  ylab = NULL,
  xlim = NULL,
  ylim = NULL,
  px = 1,
  py = 1,
  tukey = NULL,
  base = NULL,
  raw_tick = FALSE,
  show.par = TRUE,
  reg = TRUE,
  poly = 1,
  w = NULL,
  robust = FALSE,
  rlm.d = list(psi = "psi.bisquare"),
  lm.col = rgb(1, 0.5, 0.5, 0.8),
  lm.lw = 1.7,
  lm.lty = 1,
  sd = TRUE,
  mean.l = TRUE,
  asp = TRUE,
  square = TRUE,
  grey = 0.6,
  pch = 21,
  p.col = "grey50",
  p.fill = "grey80",
  size = 0.8,
  alpha = 0.8,
  q = FALSE,
  inner = 0.68,
  qcol = rgb(0, 0, 0, 0.05),
  q.type = 5,
  loe = FALSE,
  loe.lw = 1.7,
  loe.lty = 2,
  loe.col = rgb(0.3, 0.3, 1, 1),
  loess.d = list(family = "symmetric", span = 0.7, degree = 1),
  stats = FALSE,
  stat.size = 0.8,
  hline = NULL,
  vline = NULL,
  plot = TRUE,
  ...
)
}
\arguments{
\item{dat}{Optional data frame containing \code{x} and \code{y}.}

\item{x}{A numeric vector or column name in \code{dat} for the x-axis.}

\item{y}{A numeric vector or column name in \code{dat} for the y-axis.}

\item{xlab}{Optional x-axis labels. Defaults to variable names.}

\item{ylab}{Optional y-axis labels. Defaults to variable names.}

\item{xlim}{X-axis range.}

\item{ylim}{Y-axis range.}

\item{px}{Power transformation used in the input data to display if
\code{show.par = TRUE}.}

\item{py}{Power transformation used in the input data to display if
\code{show.par = TRUE}.}

\item{tukey}{Boolean determining if a Tukey transformation should be adopted
(FALSE adopts a Box-Cox transformation).}

\item{base}{Base used with the log() function if \code{px} or
\code{py} is \code{0}.}

\item{raw_tick}{Logical. If \code{TRUE}, original (untransformed) equally
spaced tick values are displayed on the re-expressed axes.}

\item{show.par}{Logical; whether to display plot parameter summary on the
plot. Currently only applies to regression model input.}

\item{reg}{Logical; whether to fit and display a regression line.}

\item{poly}{Integer; regression model polynomial degree (defaults to 1 for
linear model).}

\item{w}{Optional numeric vector of weights for regression.}

\item{robust}{Logical; if \code{TRUE}, uses robust regression (\code{MASS::rlm}).}

\item{rlm.d}{List; parameters for \code{MASS::rlm}, (e.g., \code{list(psi =
"psi.bisquare")}).}

\item{lm.col}{Regression line color.}

\item{lm.lw}{Numeric; Regression line width.}

\item{lm.lty}{Numeric; Regression line type.}

\item{sd}{Logical; whether to show ±1 SD lines.}

\item{mean.l}{Logical; whether to show x and y mean reference lines.}

\item{asp}{Logical; whether to preserve the aspect ratio (ignored if
\code{square = FALSE}).}

\item{square}{Logical; whether to create a square plotting window.}

\item{grey}{Numeric between \code{0-1}; controls grayscale background elements
(\code{0 = black}, \code{1 = white}).}

\item{pch}{Integer; point symbol.}

\item{p.col}{Point border color.}

\item{p.fill}{Point fill color.}

\item{size}{Point size.}

\item{alpha}{Point transparency level (0 = 100\\\% transparent, 1 = 100\\\% opaque).}

\item{q}{Logical; whether to draw inner quantile boxes (quantile shading).}

\item{inner}{Numeric; defines the inner fraction of values to highlight
with quantile shading.}

\item{qcol}{Fill color of quantile shading.}

\item{q.type}{Integer; type of quantile calculation (see \code{quantile}).}

\item{loe}{Logical; whether to plot loess smooth line.}

\item{loe.lw}{Numeric; Loess smooth line width.}

\item{loe.lty}{Numeric; Loess smooth line type.}

\item{loe.col}{Loess smooth color.}

\item{loess.d}{List; parameters for \code{loess.smooth}, e.g., \code{list(span = 0.7,
degree = 1)}.}

\item{stats}{Logical; if \code{TRUE}, displays model statistics (R², β, p-value).}

\item{stat.size}{Text size for \code{stats} plot display.}

\item{hline}{Numeric; location(s) of additional horizontal reference lines.
Can be passed via  the \code{c()} function.}

\item{vline}{Numeric; location(s) of additional vertical reference lines.
Can be passed via the \code{c()} function.}

\item{plot}{Logical. Generates a plot if \code{TRUE}.}

\item{...}{Additional graphical parameters (currently unused but reserved for
future expansion).}
}
\value{
If \code{reg = TRUE}, invisibly returns a list of class
\code{"eda_lm"} with the following components:
\describe{
\item{residuals}{Residuals from the model.}
\item{a}{Intercept.}
\item{b}{Slope or polynomial coefficients.}
\item{fitted.values}{Predicted values.}
\item{x}{x-values used.}
\item{x_lab}{X-axis label.}
\item{parxy}{\code{par()} parameters defining margins. Useful if user wants to add to the plot.}
}
If \code{reg = FALSE}, returns only the \code{par()} parameters defining margins.
}
\description{
Creates an enhanced scatter plot of two variables with extensive
customization for exploratory analysis. Options include polynomial regression
lines, robust regression, quantile shading, mean/sd lines, loess smoothing,
and detailed styling parameters.
}
\details{
This function serves as a flexible plotting engine for creating
scatterplots with various enhancements useful in exploratory data analysis.
}
\examples{
# Basic usage
\dontrun{
.eda_plot_xy(mtcars, x = wt, y = mpg)

# With robust regression and 2nd-degree polynomial
.eda_plot_xy(mtcars, wt, mpg, robust = TRUE, poly = 2)

# With quantiles and loess
.eda_plot_xy(mtcars, wt, mpg, q = TRUE, loe = TRUE)
}
}
\keyword{EDA}
\keyword{graphics}
\keyword{plot}
\keyword{scatterplot}
\keyword{visualization}
