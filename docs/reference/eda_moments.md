# Compute moments of a dataset

Computes the mean, variance, skewness, and excess kurtosis of a given
numeric vector.

## Usage

``` r
eda_moments(x)
```

## Arguments

- x:

  A numeric vector representing the dataset.

## Value

A named vector with the count, mean, variance, skewness, and excess
kurtosis of the data.
